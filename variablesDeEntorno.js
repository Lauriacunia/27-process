const config = require("./config.js");

console.log(`mi variable de entorno puerto es:${config.PORT}`);

/**dotenv es un modulo que carga variables de entorno
 * desde un archivo .env a process.env
 */
